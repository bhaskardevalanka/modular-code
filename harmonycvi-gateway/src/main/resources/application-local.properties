# Local developer overrides — activate with: --spring.profiles.active=local
# Set secrets (DB_PASSWORD, JWT_SECRET, etc.) via OS environment variables or .env.local

# Show SQL in dev
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true

# Local data base directory (Windows dev machine) — storage paths are derived from this
app.data-dir=C:\\Users\\sruthi.kota\\Desktop\\workspace

# Local Weasis instance
weasis.base-url=http://192.168.1.103:8084
